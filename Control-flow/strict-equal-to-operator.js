const a = 4;
//strict equal operator
console.log(a === 4);// true
console.log(a === '4');//false

//=== evaluates to true if the operands are equal  and of the same type. here 4 and '4' are the same numbers but
//the data type is different === checks data types also.